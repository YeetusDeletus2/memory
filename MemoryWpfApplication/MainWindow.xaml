<Window x:Class="MemoryWpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MemoryWpfApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="1600" Loaded="MainWindow_Loaded"> 
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" /> <!-- Auto adjusts to available space -->
            <ColumnDefinition Width="200" /> <!-- 200 pixels for labels -->
        </Grid.ColumnDefinitions>
        
        <ScrollViewer Grid.Column="0">
            <UniformGrid x:Name="ButtonUniformGrid">
                <!-- Buttons will be added here dynamically -->
            </UniformGrid>
        </ScrollViewer>

        <!-- Labels or informative content -->
        <StackPanel Grid.Column="1" Background="LightGray">
            <!-- Add informative labels here -->
            <TextBlock Margin="10" Text="Label 1" />
            <TextBlock Margin="10" Text="Label 2" />
            <!-- Add more labels as needed -->
        </StackPanel>
    </Grid>
</Window>